import styled from "styled-components";
import { Box } from '@rebass/grid';

const NavbarSisterContainer = styled(Box)`
  font-family: ${props => props.theme.fontFamily};
  margin: 0 auto;
  padding-top: 5.2rem;
`
export default NavbarSisterContainer;