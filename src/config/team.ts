export interface ITeam {
  name: string;
  members: string[];
  devpostURL?: string;
  projectName?: string;
}

export interface IMemberName {
  firstName: string;
  lastName: string;
  school: string;
  email: string;
}
